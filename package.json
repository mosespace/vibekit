{
  "name": "vibekitpro",
  "version": "1.0.0",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=vibekit-web",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.9.12"
  }
}
